DRIVER=~fastapi+~httpx+~websockets
# .env 文件
LOG_LEVEL=DEBUG  #全局日志级别 可选 DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_COLORIZE=true    # 是否启用颜色（true/false）
LOG_FORMAT=detailed  # 可选 simple/detailed（需代码中预定义格式）

QQ_IS_SANDBOX=false

QQ_BOTS='
[
  {
    "id": "xxx",
    "token": "xxx",
    "secret": "xxx",
    "intent": {
      "c2c_group_at_messages": true
    },
    "use_websocket": true
  }
]
'